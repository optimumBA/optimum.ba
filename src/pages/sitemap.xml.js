import {posts, routes} from '../lib/content.js';
export const GET = () => new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${['/blog/', ...routes].map(route => `<url><loc>https://optimum.ba${route}</loc>${route === '/portfolio/' ? '' : `<lastmod>${(posts.find(post => `/blog/${post.slug}/` === route) || posts[0]).date.toISOString()}</lastmod>`}</url>`).join('')}</urlset>`, {headers: {'Content-Type': 'application/xml; charset=utf-8'}});
